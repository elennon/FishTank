<UserControl x:Class="FishTank.Controls.Rules"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="600" >
	

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label FontSize="20" >Level One Rules</Label>
        <TextBlock Grid.Row="1"  FontSize="20" >The fish have escaped and you need to get then back in the bowl!</TextBlock>
        <TextBlock Grid.Row="2"  FontSize="20" >Click and hold to move the fish</TextBlock>
        <TextBlock Grid.Row="3"  FontSize="20" >The quicker you get them all in, the higher your score</TextBlock>
    </Grid>
</UserControl>
